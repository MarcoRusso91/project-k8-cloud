apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-app
spec:
  replicas: 6
  selector:
    matchLabels:
      app: flask
      role: app
  template:
    metadata:
      labels:
        app: flask
        role: app
    spec:
      containers:
      - name: flask
        image: my-flask-app:v3
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 5000
        env:
        - name: SERVICE_MODE
          value: "app"
        envFrom: 
        - configMapRef:
            name: flask-config
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 10